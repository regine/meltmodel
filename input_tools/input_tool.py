#/usr/bin/python

# input_tools.py
# Converts input.yaml to input.dat for Hock melt model
# Later verison will convert input.dat to input.yaml

import yaml


def yaml_to_native(config_file):
    """Converts yaml input files to native input files
        Takes in a YAML config and writes a corresponding input.dat
        to disk

        Args:
            config_file: filehandle for a yaml config file
           
        Returns: 1 if sucessful 0 otherwise, writes an input.dat to disk.
        
        Raises:

    """
    take file handle
    output file to computer
    return 

def main():
    open file handle
    give file handle to yaml_to_native()
    return
  
if __name__ == '__main__':
    main()
